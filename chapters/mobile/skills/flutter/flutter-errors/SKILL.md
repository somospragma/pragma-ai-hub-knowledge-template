---
name: flutter-errors
description: >
  Advanced error handling skill for Flutter with Dart 3.3+, using fpdart (Either/TaskEither), exception hierarchy with sealed classes, integration with Riverpod and BLoC/Cubit, and global error handling with FlutterError.onError and PlatformDispatcher. Use this skill whenever the user mentions errors in Flutter, exceptions, Either pattern, Result type, handling failures in REST APIs with Dio, Firebase, local databases (ObjectBox, Drift/SQLite), unexpected crashes, or when asking for a file structure for error handling. Also applies when the user wants to improve their error architecture, refactor scattered try/catch blocks, or implement error boundaries. Use it even if the user only mentions 'how to handle errors in Flutter' without specifying the exact pattern. Stack: Dart 3.3+, fpdart, flutter_bloc, riverpod_annotation.
commands:
  - setup-error-handling
inputs:
  - name: action
    description: Action to perform (implement, audit, add-source). "implement" generates the full error handling architecture (Failure hierarchy, ErrorHandler, global handler, interceptor), "audit" checks existing error handling for bare try/catch, missing mappings, or hardcoded error strings, "add-source" adds error mapping for a new data source (firebase, objectbox, drift, custom).
    required: true
  - name: target
    description: Path to the core/errors directory or feature to integrate (e.g. lib/core/errors/ for implement, lib/ for audit, lib/features/chat/data/ for add-source).
    required: true
  - name: source
    description: Data source to add error mapping for (dio, firebase, objectbox, drift, custom). Required when action is "add-source".
    required: false
metadata:
  author: Pragma Mobile Chapter
  version: "2.1"
---

# Flutter Advanced Error Handling

Defines the rules and best practices for error handling in Flutter applications.

## Design Principles

These principles explain the *why* behind each decision — keep them in mind when generating code:

- **Errors are values, not exceptions.** Using `Either<Failure, T>` instead of `throw` lets the compiler enforce error handling at the call site, eliminating silent omissions.
- **The domain does not know the error source.** Mapping to `Failure` in the datasource before the error propagates upward keeps the domain layer clean and testable without dependencies on Firebase, Dio, or SQLite.
- **Every error has an explicit type.** `sealed class` makes pattern matching exhaustive: if you add a new error type, the compiler flags every place that ignores it.
- **Error messages are resolved in the UI.** `Failure` carries a `FailureMessageKey`, not a hardcoded `String`, because the locale is only known in the presentation layer where `BuildContext` exists.
- **Unexpected crashes are always captured.** Registering `FlutterError.onError` and `PlatformDispatcher.onError` in `main()` ensures no error escapes without being logged.

---

## Project File Structure

```
lib/
├── core/
│   ├── errors/
│   │   ├── failures.dart              ← Sealed Failure hierarchy + FailureMessageKey
│   │   ├── exceptions.dart            ← AppException per technical source
│   │   ├── error_handler.dart         ← Exception → Failure mapper
│   │   └── global_error_handler.dart  ← FlutterError + PlatformDispatcher
│   ├── network/
│   │   └── dio_error_interceptor.dart ← Converts DioException → AppException
│   ├── l10n/
│   │   └── generated/                 ← Generated by flutter gen-l10n
│   └── widgets/
│       └── failure_view.dart          ← Reusable error widget with i18n
│
├── l10n/
│   ├── app_en.arb                     ← Error messages in English (template)
│   └── app_es.arb                     ← Error messages in Spanish
│
└── features/[feature]/
    ├── data/datasources/
    │   └── [x]_datasource.dart        ← TaskEither.tryCatch + ErrorHandler.map
    ├── domain/repositories/
    │   └── [x]_repository.dart        ← Interface: Either<Failure, T>
    └── presentation/
        ├── providers/                 ← Riverpod: AsyncNotifier with fold()
        └── bloc/                      ← BLoC: sealed states with Failure
```

---

## Error Flow: from source to UI

```
[Dio / Firebase / ObjectBox / Drift]
        ↓  throws technical exception (DioException, FirebaseException, etc.)
[Interceptor / Datasource]
        ↓  TaskEither.tryCatch → ErrorHandler.map → Failure with FailureMessageKey
[Repository]
        ↓  returns Either<Failure, T> to the domain
[UseCase → Provider / BLoC]
        ↓  calls .run() and performs explicit fold()
[UI Widget]
        ↓  failure.localizedMessage(context) → String translated to active locale
```

---

## Reference Files

Read the corresponding file before generating code for that layer:

| What to implement | Reference |
|---|---|
| `Failure`, `FailureMessageKey`, UI extensions | `references/failures.md` |
| `AppException`, `ErrorHandler`, HTTP/Firebase/SQLite mappings | `references/exceptions.md` |
| `FailureMessageKey` enum, `Failure` with i18n, `FailureView` | `references/failure_message_key.md` |
| ARB (en/es), `l10n.yaml`, localizations configuration | `references/i18n_errors.md` |
| ObjectBox and Drift datasources with `TaskEither`, reactive streams | `references/local_db_datasources.md` |
| `AsyncNotifier`, `FutureProvider`, `FailureView` with Riverpod | `references/riverpod_integration.md` |
| `Cubit`, `BLoC`, `BlocBuilder`/`BlocListener` with `Failure` | `references/bloc_integration.md` |
| `FlutterError.onError`, `PlatformDispatcher`, `BlocObserver` | `references/global_errors.md` |
| `DioErrorInterceptor`, `AuthInterceptor`, Dio client with Riverpod | `references/dio_interceptor.md` |

> `local_db_datasources.md` has over 300 lines — it includes a table of contents at the top.

---

## Base Pattern in Datasources

All datasources follow this pattern. `ErrorHandler.map` centralizes the mapping so the datasource does not need to know the error translation rules:

```dart
TaskEither<Failure, T> fetchSomething() => TaskEither.tryCatch(
  () async { /* actual call */ },
  (error, stackTrace) => ErrorHandler.map(error, stackTrace),
);
```

---

## Pre-delivery Checklist

- [ ] Datasource uses `TaskEither.tryCatch` with `ErrorHandler.map`
- [ ] Dio errors pass through `DioErrorInterceptor` before reaching the datasource
- [ ] Repository returns `Either<Failure, T>` — no own `throw`
- [ ] BLoC / Notifier performs explicit `fold()` — not `getOrElse` that hides the Left
- [ ] Error state carries a `Failure`, not a `String`
- [ ] `GlobalErrorHandler.initialize()` is called in `main()` before `runApp`
- [ ] Firebase exceptions mapped from `FirebaseException.code`
- [ ] ObjectBox errors wrapped in `ObjectBoxException`
- [ ] Drift errors wrapped in `DriftException` with `sqliteCode` if available
- [ ] `Failure` uses `FailureMessageKey` — no hardcoded `String message`
- [ ] UI calls `failure.localizedMessage(context)` — never accesses `literalMessage` directly
- [ ] Retry button uses `l.retryButton` from the ARB
- [ ] Every new key exists in **both** `app_en.arb` and `app_es.arb`
- [ ] Use absolute imports with `package:` — never relative imports (`import '../..'`)
- [ ] `flutter gen-l10n` was run after modifying the ARB files
